# Copie este arquivo para .env e preencha os valores.
#   cp .env.example .env
# O .env e ignorado pelo git e nunca deve ser versionado.

# Chave secreta do agente playit.gg, usada pelo servico "playit".
# Obtenha em https://playit.gg -> Account -> Secret Key.
PLAYIT_SECRET_KEY=
